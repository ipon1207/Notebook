## 文字列の長さ

文字列の長さ（文字数）は `文字列変数.size()` で取得できる

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string str = "Hello";
    cout << str.size() << endl;
}
```

**実行結果**

```PlainText
5
```
