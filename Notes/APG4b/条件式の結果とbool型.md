- 条件式の結果は真のとき `1`、偽のとき `0` になる
- `1` を `true`、`0` を `false` で表す
- bool 型は `true` または `false` の 2 つの値のみを取るデータ型

|          | int 型の表現 | bool 型の表現 |
| -------- | ------------ | ------------- |
| 真の場合 | 1            | true          |
| 偽の場合 | 0            | false         |

```cpp

```
